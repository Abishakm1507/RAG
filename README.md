# RAG Project

This repository appears to be part of an AI championship project involving
Retrieval-Augmented Generation (RAG). The workspace currently contains:

- `myvenv/` – Python virtual environment created for development
- `docker/` – Docker configuration (e.g. `Dockerfile`)

## Getting started

1. **Activate the virtual environment** (PowerShell):
   ```powershell
   Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
   .\myvenv\Scripts\Activate.ps1
   ```
   or use `myvenv\Scripts\python` directly.

2. **Install dependencies**:
   ```powershell
   python -m pip install -r requirements.txt
   ```

3. **Run the application** – instructions will depend on the project’s entry point.

## Repository structure

- `docker/` – Docker build files
- `myvenv/` – ignored in version control

## Notes

- Add source code, tests, and documentation as the project evolves.
- Modify this README with details relevant to usage, development, and deployment.
